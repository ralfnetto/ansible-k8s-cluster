---
  - name: Add worker to cluster
    shell: #add command generated by rancher

  # - name: Pause until node is ready on Rancher interface
  #   pause:
  #     prompt: "Press [ENTER] when node is ready on Rancher interface"